import random
print("Choose the minimum number and the maximum number.(ONLY INTEGERS)")
minimum=int(input("Minimum:"))
maximum=int(input("Maximum:"))
result=(random.randint(minimum,maximum))
print(result)
